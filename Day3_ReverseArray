class Solution {
    public void reverseArray(int arr[]) {
        // code here nipun_aggarwal
        int s = arr.length;

        for(int i=0;i<s/2;i++)
        {
            int temp = arr[i];
            arr[i] = arr[s-i-1];
            arr[s-i-1] = temp;
        }

    }
}
